export interface AccountType {
    id: string;
    access_token: string;
    refresh_token: string;
    expires_in: string;
    username: string;
    full_name: string;
    company_id: boolean;
    role: string;
}